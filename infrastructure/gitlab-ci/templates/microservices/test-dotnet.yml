test:
  stage: test
  script:
    - echo "running tests..."

  #artifacts:
  #  when: always
  #  paths:
  #    - $PROJECT_DIR/build/reports/tests/test
  #  reports:
  #    junit:
  #      - $PROJECT_DIR/build/test-results/test/*.xml