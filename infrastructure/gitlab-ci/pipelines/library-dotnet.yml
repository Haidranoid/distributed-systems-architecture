include:
  - local: $GITLAB_CI_BOOTSTRAP_DIR/setup-dotnet.yml
  - local: $GITLAB_CI_TEMPLATES_LIBRARIES_DIR/test-dotnet.yml
  - local: $GITLAB_CI_TEMPLATES_LIBRARIES_DIR/publish-dotnet.yml
  - local: $GITLAB_CI_TEMPLATES_LIBRARIES_DIR/deploy-dotnet.yml

stages:
  - test
  - publish
  - deploy

default:
  tags:
    - local
    - dotnet
    - library