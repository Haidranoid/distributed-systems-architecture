include:
  local: infrastructure/gitlab-ci/bootstrap/base.yml

default:
  image:
    name: haidranoid/gitlab-runner-dotnet10:latest
    pull_policy: if-not-present

  tags:
    - local
    - dotnet

variables:
  PROJECT_DOCKERFILE: $PROJECT_NAME_SOLUTION/Dockerfile