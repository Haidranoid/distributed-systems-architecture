default:
  before_script:
    - . bootstrap.sh
    - aws_login v1
    - aws_load_env_vars
    - docker_login
    - cd $PROJECT_DIR
    - echo "$(pwd)"
    - echo "$PROJECT_DIR"
    - echo "$PROJECT_DOCKERFILE"