default:
  image:
    name: haidranoid/gitlab-runner-java21:latest
    pull_policy: if-not-present

  before_script:
    - cd infrastructure/scripts

    - . setup-env.sh

    - aws_login v1
    - load_aws_env_vars

    - cd ../../