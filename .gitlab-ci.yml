variables:
  GIT_DEPTH: 0
  
  GRADLE_USER_HOME: /cache/gradle/$PROJECT_NAME
  GRADLE_OPTS: >-
    -Dorg.gradle.daemon=false
    -Dorg.gradle.parallel=true
    -Dorg.gradle.caching=true
  
  INFRA_DIR: infra
  ARTIFACTS_DIR: artifacts
  SERVICES_DIR: services
  GITLAB_DIR: $INFRA_DIR/ci/gitlab
  GITLAB_TEMPLATES_DIR: $INFRA_DIR/ci/gitlab/templates

stages:
  - artifacts
  - services

trigger-shared-starter:
  stage: artifacts
  variables:
    PROJECT_NAME: shared-starter
    PROJECT_DIR: $ARTIFACTS_DIR/$PROJECT_NAME
  trigger:
    include:
      - local: $PROJECT_DIR/.gitlab-ci.yml
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - $PROJECT_DIR/**/*
        - $GITLAB_TEMPLATES_DIR/$PROJECT_DIR/**/*

trigger-config-server:
  stage: services
  variables:
    PROJECT_NAME: config-server
    PROJECT_DIR: $SERVICES_DIR/$PROJECT_NAME
  trigger:
    include:
      - local: $PROJECT_DIR/.gitlab-ci.yml
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - $PROJECT_DIR/**/*
        - $GITLAB_TEMPLATES_DIR/$PROJECT_DIR/**/*

trigger-accounts-service:
  stage: services
  variables:
    PROJECT_NAME: accounts-service
    PROJECT_DIR: $SERVICES_DIR/$PROJECT_NAME
  trigger:
    include:
      - local: $PROJECT_DIR/.gitlab-ci.yml
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - $PROJECT_DIR/**/*
        - $GITLAB_TEMPLATES_DIR/$PROJECT_DIR/**/*

trigger-authentication-service:
  stage: services
  variables:
    PROJECT_NAME: authentication-service
    PROJECT_DIR: $SERVICES_DIR/$PROJECT_NAME
  trigger:
    include:
      - local: $PROJECT_DIR/.gitlab-ci.yml
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - $PROJECT_DIR/**/*
        - $GITLAB_TEMPLATES_DIR/$PROJECT_DIR/**/*

trigger-gateway:
  stage: services
  variables:
    PROJECT_NAME: gateway
    PROJECT_DIR: $SERVICES_DIR/$PROJECT_NAME
  trigger:
    include:
      - local: $PROJECT_DIR/.gitlab-ci.yml
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - $PROJECT_DIR/**/*
        - $GITLAB_TEMPLATES_DIR/$PROJECT_DIR/**/*