include:
  - local: '/artifacts/shared-starter/.gitlab-ci.yml'
  - local: '/services/config-server/.gitlab-ci.yml'

stages:
  - test
  - publish

default:
  tags:
    - local

variables:
  GIT_DEPTH: 0