trigger-shared-starter-workflow:
  trigger:
    include:
      - local: '/artifacts/shared-starter/.gitlab-ci.yml'
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - artifacts/shared-starter/**/*
        - infra/ci/gitlab/templates/artifacts/shared-starter/**/*
        - infra/ci/gitlab/templates/setup-env.yml

trigger-config-server-workflow:
  trigger:
    include:
      - local: '/services/config-server/.gitlab-ci.yml'
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - services/config-server/**/*
        - infra/ci/gitlab/templates/images/config-server/**/*
        - infra/ci/gitlab/templates/setup-env.yml

trigger-accounts-service-workflow:
  trigger:
    include:
      - local: '/services/accounts-service/.gitlab-ci.yml'
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - services/accounts-service/**/*
        - infra/ci/gitlab/templates/images/accounts-service/**/*
        - infra/ci/gitlab/templates/setup-env.yml

trigger-authentication-service-workflow:
  trigger:
    include:
      - local: '/services/authentication-service/.gitlab-ci.yml'
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - services/authentication-service/**/*
        - infra/ci/gitlab/templates/images/authentication-service/**/*
        - infra/ci/gitlab/templates/setup-env.yml

trigger-gateway-workflow:
  trigger:
    include:
      - local: '/services/gateway/.gitlab-ci.yml'
  rules:
    - if: "$CI_COMMIT_BRANCH == 'master'"
      changes:
        - services/gateway/**/*
        - infra/ci/gitlab/templates/images/gateway/**/*
        - infra/ci/gitlab/templates/setup-env.yml

variables:
  GIT_DEPTH: 0