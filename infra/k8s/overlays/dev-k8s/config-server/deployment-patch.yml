apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: config-server
          imagePullPolicy: Never
          