default:
  image:
    name: haidranoid/gitlab-ci-runner-dotnet10:latest
    pull_policy: if-not-present

  before_script:
    - cd infra/scripts

    - . setup-env.sh

    - aws_login v1
    - load_aws_env_vars

    - cd ../../