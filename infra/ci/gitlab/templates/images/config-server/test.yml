include:
  - local: '/infra/ci/gitlab/templates/setup-env.yml'

test:
  stage: test
  extends:
    - .setup-gradle
  image:
    name: haidranoid/gitlab-ci-java21:latest
    pull_policy: if-not-present

  script:
    - cd services/config-server
    - chmod +x ./gradlew
    - ./gradlew test --build-cache --parallel

  artifacts:
    when: always
    paths:
      - services/config-server/build/libs/*.jar
      - services/config-server/build/reports/tests/test
    reports:
      junit:
        - services/config-server/build/test-results/test/*.xml