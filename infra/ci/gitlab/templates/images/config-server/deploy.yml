include:
  - local: '/infra/ci/gitlab/templates/setup-env.yml'

config-server-deploy:
  stage: deploy
  extends:
    - .setup-credentials
    - .setup-image
  needs:
    - job: config-server-publish-image
      artifacts: true

  script:
    - cd $K8S_DIR/overlays/dev-k8s/config-server

    - kustomize edit set image $PROJECT_NAME=$IMAGE_ECR_TAG

    - kubectl apply -k .