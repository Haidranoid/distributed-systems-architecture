default:
  image:
    name: haidranoid/gitlab-ci-java21:latest
    pull_policy: if-not-present

  before_script:
    - cd infra/scripts

    - echo "$CI_CONFIG_PATH"
    - . setup-env.sh

    - aws_login v1
    - load_env_vars

    - cd ../../