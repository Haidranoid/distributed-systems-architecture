default:
  image:
    name: haidranoid/gitlab-ci-java21:latest
    pull_policy: if-not-present

  before_script:
    - cd infra/scripts

    - . setup-env.sh
    - echo $PROJECT_DIR
    - load_env_vars "$PROJECT_DIR"

    - cd ../../