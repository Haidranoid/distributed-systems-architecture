spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/accounts_service_dev
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update