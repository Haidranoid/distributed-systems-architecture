include:
  - local: '$INFRA_DIR/ci/gitlab/setup-env.yml'
  - local: '$INFRA_DIR/ci/gitlab/templates/$PROJECT_DIR/test.yml'
  - local: '$GITLAB_TEMPLATES_DIR/$PROJECT_DIR/publish.yml'

stages:
  - test
  - publish

default:
  tags:
    - local