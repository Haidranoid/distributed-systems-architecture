server:
  port: 8081
spring:
  application:
    name: accounts-service
  profiles:
    active: dev
  config:
    import: optional:configserver:http://config-server:8888
management:
  endpoints:
    web:
      exposure:
        include: health, info