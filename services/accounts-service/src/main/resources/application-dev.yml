spring:
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5432/accounts_service_dev
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update