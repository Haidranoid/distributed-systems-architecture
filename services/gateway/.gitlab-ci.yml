include:
  - local: '/infra/ci/gitlab/templates/images/gateway/test.yml'
  - local: '/infra/ci/gitlab/templates/images/gateway/publish.yml'

default:
  tags:
    - local

variables:
  SERVICE_NAME: gateway
  SERVICE_DIR: services/$SERVICE_NAME
  SERVICE_RELATIVE_PATH_DIR: /$SERVICE_DIR/$SERVICE_DIR

  GRADLE_USER_HOME: $SERVICE_RELATIVE_PATH_DIR/.gradle
  GRADLE_OPTS: >-
    -Dorg.gradle.daemon=false
    -Dorg.gradle.parallel=true
    -Dorg.gradle.caching=true

stages:
  - test
  - publish