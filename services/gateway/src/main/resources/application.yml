server:
  port: 8222
spring:
  application:
    name: gateway
  profiles:
    active: dev
  threads:
    virtual:
      enabled: true
  config:
    import: optional:configserver:http://config-server:8888
management:
  endpoints:
    web:
      exposure:
        include: health, info