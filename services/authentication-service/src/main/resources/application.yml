server:
  port: 8080
spring:
  application:
    name: authentication-service
  profiles:
    active: dev
  config:
    import: optional:configserver:http://config-server:8888
management:
  endpoints:
    web:
      exposure:
        include: health, info