spring:
  config:
    import: optional:configserver:http://host.docker.internal:8888
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/authentication_service_dev
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
application:
  endpoints:
    accounts-service-endpoint: http://host.docker.internal:8081/api/v1/accounts
    accounts-service-internal-endpoint: http://host.docker.internal:8081/api/v1/internal/accounts